{%- comment -%}
  invariant: Canonical rules live in assets/theme.bundle.css; this asset is served as theme.css and must @import a different filename (Shopify: avoid asset_url("theme.css") inside theme.css.liquid — circular / empty stylesheets in some CDNs).
  invariant: @font-face and :root --theme-font-* are emitted by snippets/theme-font-faces.liquid (before this stylesheet_tag in layout).
  invariant: Do not re-expand font_face or duplicate bundle CSS here.
{%- endcomment -%}

@import url('{{ "theme.bundle.css" | asset_url }}');
