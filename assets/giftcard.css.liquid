{%- comment -%}
  invariant: Canonical gift card rules live in assets/giftcard.css; typography @font-face output lives in snippets/gift-card-font-faces.liquid (see templates/gift_card.liquid).
  invariant: This asset is a thin shim so legacy links to giftcard.css.liquid keep working; do not re-expand a full duplicate theme CSS here (compile cost + drift + empire Layout desync).
  invariant: If a custom layout loads ONLY this file, it must still render gift-card-font-faces before this stylesheet (same ordering as gift_card.liquid) so --giftcard-font-* variables exist.
{%- endcomment -%}

@import url('{{ "giftcard.css" | asset_url }}');
