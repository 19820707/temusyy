{%- comment -%}
  invariant: Canonical MDC ripple + storefront a11y (focus-visible, prefers-reduced-motion) live in assets/ripple.css.
  invariant: This asset is a thin shim so any legacy or alternate reference to ripple.css.liquid still resolves; do not re-expand full vendor CSS here (compile cost + drift + desync from ripple.css hardening).
{%- endcomment -%}

@import url('{{ "ripple.css" | asset_url }}');
